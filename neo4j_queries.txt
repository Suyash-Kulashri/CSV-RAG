"""
Helper script to generate useful Neo4j queries for viewing your data.
"""
print("=" * 60)
print("Useful Neo4j Browser Queries")
print("=" * 60)

print("\n1️⃣ View ALL nodes and relationships:")
print("""
MATCH (n)-[r]->(m)
RETURN n, r, m
LIMIT 50
""")

print("\n2️⃣ View all Models with their Parts:")
print("""
MATCH (m:Model)-[:HAS_PART]->(p:Part)
RETURN m, p
LIMIT 100
""")

print("\n3️⃣ View a specific Model and its Parts:")
print("""
MATCH (m:Model {name: 'YOUR_MODEL_NAME'})-[:HAS_PART]->(p:Part)
RETURN m, p
""")

print("\n4️⃣ Count nodes by type:")
print("""
MATCH (n)
RETURN labels(n)[0] as label, count(n) as count
ORDER BY count DESC
""")

print("\n5️⃣ View all Models:")
print("""
MATCH (m:Model)
RETURN m
LIMIT 50
""")

print("\n6️⃣ View all Parts:")
print("""
MATCH (p:Part)
RETURN p
LIMIT 50
""")

print("\n7️⃣ View graph statistics:")
print("""
MATCH (n)
RETURN count(n) as total_nodes

MATCH ()-[r]->()
RETURN count(r) as total_relationships

MATCH (m:Model)-[r:HAS_PART]->(p:Part)
RETURN count(r) as model_part_relationships
""")

print("\n💡 Tip: In Neo4j Browser, paste these queries and click 'Run'.")
print("   To see more nodes, increase the LIMIT number or remove LIMIT.")
print("   Click on nodes to expand and see their connections!")

